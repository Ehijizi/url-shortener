# Module composition for the dev environment.
# Modules will be added here as Phase 1 progresses:
#   - dynamodb (URLs table + analytics table)
#   - lambda (one per service: shortener, redirect, analytics)
#   - api (API Gateway HTTP API)
